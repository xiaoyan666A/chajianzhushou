<?xml version="1.0" encoding="utf-8"?>
<!-- 更新下载进度弹窗内容：与 App 主题一致（AlertDialog 套用 Theme.Chajianzhushou.Dialog） -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="4dp"
    android:paddingEnd="24dp"
    android:paddingBottom="20dp">

    <TextView
        android:id="@+id/download_progress_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="正在连接更新服务器..."
        android:textColor="@color/ink"
        android:textSize="@dimen/txt_sm" />

    <ProgressBar
        android:id="@+id/download_progress_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:layout_marginTop="16dp"
        android:max="100"
        android:progressBackgroundTint="@color/hair2"
        android:progressTint="@color/accent" />

    <TextView
        android:id="@+id/download_progress_percent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:gravity="end"
        android:text="0%"
        android:textColor="@color/accent"
        android:textSize="@dimen/txt_base"
        android:textStyle="bold" />

</LinearLayout>
